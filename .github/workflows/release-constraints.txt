# Exact dependency contract for the ledger tag-release gate.
# Every tool the release gate installs or resolves — the declared build/
# validation tools, their transitive runtime set, and the build-isolation
# and wheel-smoke toolchain — is pinned here so one immutable tag always
# validates under the same dependency identities.
build==1.5.0
certifi==2026.7.22
docutils==0.23
idna==3.19
jaraco-classes==3.4.0
keyring==25.7.0
more-itertools==11.1.0
nh3==0.3.7
packaging==26.3
pyproject-hooks==1.2.0
requests==2.34.2
rfc3986==2.0.0
rich==15.0.0
setuptools==84.0.0
twine==7.0.0
urllib3==2.7.0
wheel==0.48.0
jaraco-context==6.1.2
jaraco-functools==4.6.0
